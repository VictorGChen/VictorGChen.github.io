# Gemfile
# -----------
# This file defines the Ruby gems (dependencies) needed to build the site.
# You typically don't need to modify this much.
# If you have Ruby installed locally:
#   bundle install
#   bundle exec jekyll serve
# to run the site at http://localhost:4000

source "https://rubygems.org"

# github-pages:
#   A meta-gem maintained by GitHub that pins specific Jekyll + plugin versions
#   to match what GitHub Pages uses on their servers.
gem "github-pages", group: :jekyll_plugins

# webrick:
#   Required to run `jekyll serve` on modern Ruby versions.
gem "webrick", "~> 1.8"
